---
"which-pm": minor
"read-yaml-file": minor
---

The `yaml` NPM package is now used for reading instead of `js-yaml`. There should be no behavior differences in this change. This migration supports a broader migration in pnpm from `js-yaml` to `yaml`.
